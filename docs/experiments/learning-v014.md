# v0.14：工作学习诊断、有限在线对照与数值修订

**本轮补齐了真实分组学习信号、可操作路径、显式事实与同终局合同的时间信用对照；但预定多窗口学习比较未完成。**六个正式运行全部触发原概率一致性停止条件，训练后开发/锁定评价均未执行，不能判断MC与RTG谁改善了工作。失败记录保留；没有用完成了3次更新替代学习收益结论。

依据为[v0.13审计](../reference/learning-audit-v013.md)与[事前计划](../learning-v014-plan.md)。详细机器记录分别为[正式计数](v014-study-high-summary.json)、[信号](v014-study-high-signals.json)、[真实工作](v014-study-high-work-summary.json)和[数值修复诊断](v014-numeric-diagnostic.json)。原始运行仍在服务器`runs/learning-v014-*`，派生分析不重新执行SQL、改变原回报或覆盖历史。

## 审计要求的落实

| 要求 | 本轮实现 | 证据边界 |
|---|---|---|
| 区分保留失败与有效信号 | 历史两窗只读分解；真实更新按任务/成员/过去公共阶段记录优势、token、loss、真梯度、概率变化 | 非零信号不等于工作收益；旧缺失梯度不补造 |
| 判断公开路径是否可完成 | 40注册案例，50条规则合理路径、6条负例；真实请求容量检查；六例三臂呈现/上下文对照 | 规则路径不是模型学习成功 |
| 改善过去奖励信用 | 同一终局合同的MC与联合RTG，真实事件早结算与终点补差 | 基础配方，非ID-VTDO创新或严格无偏等价证明 |
| 多窗、独立种子和当前支持 | 容量后冻结两条件×3seed×4窗×16例；每ξ当窗4次 | 实际数值停止，未完成预定学习曲线 |
| 拆开事实与信息因子 | 期间、状态、倍数、去重、半开订单边界、资料持有者、成果正确性显式登记 | 一个构造家族，非独立来源泛化 |
| 依据驱动行动和恢复 | 读取依据、改SQL/结果、执行确切版本、有据复核与具体义务恢复的只读诊断 | 混合样本描述不作为初始/最终收益差 |

## 冻结与执行范围

E0/容量源`00b5df83ea0f1001e54a5cdbcdb11ca9da2afe55`；正式源`0dda1bff05289db9ae213bc449d8995ec80fb15e`；数值修订源`e6cf0aa`。分别在独立冻结worktree运行，后续只读报告脚本不改变正在执行的代码。历史v0.13基线`fd237a3f3bf317906c023ad0f09b76920dc06f62`不覆盖。

固定本地Qwen2.5-7B-Instruct文件manifest，一套共享actor/LoRA/actor optimizer服务所有岗位，独立critic。每个seed是该方案的一次独立重复，不是为三个岗位分别训练权重。正式起点为未训练过本轮素材的base/初始LoRA，不使用容量更新后的权重。全部使用resident_direct，实际网络HTTP=0。

公开返回采用v14同一schema；compact只投影submit与默认inspect的可检索合同结构，保留确切产物、状态和真正的完整合同检索。read正文、SQL结果和拒绝不压缩。原返回与公开返回按实际actor/project/tool/args/profile绑定，确定性重算核验；没有自动读取、采用或返回参考SQL。现有WorldCore.observe本身不含固定提交大快照，因此没有假称压缩了不存在的观察字段。

## E0：历史信号和可操作性

[历史分析](../online-credit-v014.md)确认：旧首窗实现7决定/522输出token、复核3/136均零优势且自身actor项为零；整链实际交接后14决定/912 token仍获+0.2。次窗失败已有约−0.004至−0.0054负优势，不能继续称全零。共享参数仍可被别的工作项更新；旧记录没有分组梯度，不能据loss或token比例推造。

[CPU工作路径](learning-work-v014-cpu.json)包含40注册案例主动路径与10条请求后团队路径，另6条负对照。实际round-robin与任务期限内路径成立，56条ledger均守恒于原终局合同；程序见证不计为模型收益。

[受控信息对](learning-information-v014-actual-requests.json)以隔离反事实世界保证取得区分资料前实际请求逐字相同，真实交接/读取后请求不同且正确业务值不同。它使用明确fake transport，无模型前向，不作为Qwen信息适应证据。

[规则路径实际请求容量](learning-capacity-v014-cpu.json)含真实tokenizer/schema/完整历史和512输出预留。实现/复核/整链full峰值9035/9869/10536，compact峰值8423/9302/9908；均可容纳于12288。8k会阻断必要raise_issue/approve。此检查不采样、不导出训练标签，也不把先完成全部任务作为学习准入要求。

## E0：公开返回与上下文真实对照

详细证据见[独立报告](learning-v014-e0-presentation.md)。三臂各6个开发情境，同一schema、提示、决策上限、初始权重与case抽样seed；full8/compact8预声明差异为呈现，compact8/compact12为上下文。不同世界UUID使真实输入非逐字配对，部分动作在压缩返回前已分叉，因此只作有限描述，不将所有差异唯一归因于处理因素。

| 条件 | 生成 / direct attempt | 输入token | 输出token | ctx角色停点 | R顺序 |
|---|---:|---:|---:|---:|---|
| full8 | 50 / 57 | 279704 | 3046 | 7 | 0,0,.2,0,0,0 |
| compact8 | 61 / 68 | 351009 | 3983 | 7 | 同上 |
| compact12 | 83 / 83 | 542304 | 5667 | 0 | 同上 |

局部返回字节下降，但可执行轨迹更长、总token上升，不能写成总成本节省。12k本组六例无ctx拒绝，业务得分仍逐项不变。full8与compact12各有“缺data adoption→采用data→实际build”的义务恢复，未恢复完整依据/正确成果。compact12某复核先读旧result v1，随后确实改读固定v2，仍缺充分依据；不能说始终没有改正版本。

## 原容量与正式固定预算

high容量四例耗时502.892894秒，其中采集248.419秒，41次真实生成。41条行为和反向概率核验均通过max0.02/mean0.002；最大差0.01814938接近边界，最大逐决定均差0.00067563。容量通过后来被证明不足以保证大窗跨情境稳定性，这是本轮规模选择的实际限制。

容量MC的R为`[1,0,0,.2]`；实现10决定/878输出token、复核8/1030零优势，实测自身组梯度也为0。共有13组真梯度无遗漏，总范数0.0891112971，组重构残差约1.15e−8。范数不可加；点积/余弦只显示本次方向关系，不是工作收益因果Contribution。

固定选12个首次组决定进行更新后前向，543个所选输出token；概率比约[.9671,1.0801]，最大绝对logp变化.07708，k3代理均值1.339e−5，无实测clipping。old是更新前完整序列重算、非原缓存分母，另有行为概率门；这不是full/无偏KL或多epoch稳定性证据。未选第13组为未知，见[容量信号](v014-capacity-signal-review.json)。

事前以容量×4×窗数×6运行×1.25外推：4窗约16.7631训练GPU进程小时，6窗25.1446超过自主选定24小时估计上限，故冻4窗。训练384例，评价360例另计，含裕量总估24.53 GPU进程小时。估计不代表实际GPU计算用量。

正式MC/RTG各3seed（2026092711/2026092729/2026092747），每窗四任务等份额、每任务固定精确ξ重复4次，f0/f1/f0/f1轮换。compact12k、输出512、T.7、LoRA r8 alpha16 q/v、actor_lr1e−5、critic_lr1e−3、每窗一次累积更新、clip.2、entropy/KL0一致，仅时间信用不同。开发固定在0/2/4窗；锁定新事实/持有者交换/规则各8例共24例。每seed初始锁定只由MC运行一次供配对RTG共用，不能重复计样本。

三个seed的实际初始actor、critic与两个optimizer指纹均匹配；全部已执行评价guard显示学习状态未变、CPU/CUDA RNG恢复一致。评价不执行训练前向/反向、不挑checkpoint。世界UUID仍不同，共用基线是同策略同业务条件分布测量，不是逐字配对。

## 正式六运行的实际停止

| 条件 / seed | 已闭合训练例 | 已闭合评价例 | actor / critic步数 | 停止训练窗（从0计） | 最坏概率差 |
|---|---:|---:|---:|---:|---:|
| MC / 0 | 32 | 32 | 1 / 1 | 1 | .02018094 |
| RTG / 0 | 32 | 8 | 1 / 1 | 1 | .03244184 |
| MC / 1 | 16 | 32 | 0 / 0 | 0 | .03228247 |
| RTG / 1 | 32 | 8 | 1 / 1 | 1 | .02090001 |
| MC / 2 | 16 | 32 | 0 / 0 | 0 | .03204238 |
| RTG / 2 | 16 | 8 | 0 / 0 | 0 | .03902316 |

合计计划744、闭合264（训练144+评价120）、未启动480；已闭合回报均可判定。3225次direct attempt、3221次真实生成，输入20053089、输出234641 token，4次ctx预生成拒绝。12k没有消除所有其他轨迹的上下文风险，E0无ctx只是六例范围。

六次失败均在behavior预反向门：该窗backward=0、actor/critic新增step=0，先前成功更新合计各3步。共有15个失败call/15个token超过max门，其中MC seed1另有mean门超限；代表输入3913–6161，并非只在12k边缘失败。没有删掉问题token、放宽阈值或用重算覆盖原采样概率。

训练后开发测量（2/4窗）及locked-final全部未启动，原计划项的R为null。不能把训练窗更高R当冻结收益曲线，不能给MC/RTG排优劣，也不能声称在线RL或ID-VTDO无效。原进程exit0仅表示runner正常处理停止条件，scheduler准确记为`finished_with_incomplete_jobs`。

## 真实信号、工作行为与当前支持

三个成功更新全为各run的首训练窗，critic初始为0，共55个实际阶段组（20/18/17）完整记录，总梯度分别.04819424/.02493480/.04469308，组重构残差4.05e−9至9.10e−9。短交接组与总梯度的有符号对齐份额约.99563/.97758/.99298；它不是可加范数比例或工作收益归因。

MC seed0已送达阶段仍有17决定/1924输出token为正优势且组梯度非零；RTG两首窗相应142决定/10902 token全零优势且实测组梯度0。这证明候选确实移除了过去交接分对后续动作的当前直接强化项；不同原轨迹和幸存窗口限制了比较，不能据此称RTG改善工作。独立复核在三个成功窗均零自身非零actor信号；实现部分正信号只来自原合同读取项，未产生正确交付。

三个post各固定选12决定，覆盖445/549/520输出token，概率比范围分别[.95251,1.11858]/[.95180,1.02770]/[.94682,1.02276]，实际反向和post均无clipping。更多任务/成员/阶段的正负零计数见[逐窗说明](v014-study-high-signal-note.md)，未测梯度和post仍为未知。

[工作诊断](v014-study-high-work.md)逐条核对原manifest/trace/state/receipt，不将后续ok当作纠错。训练与初始评价采用不同案例并混有不同θ，下表描述实际瓶颈，不是收益前后差。

| 工作事实 | 已采训练 | 初始评价 |
|---|---:|---:|
| 实现读适用basis | 14/36 | 14/30 |
| 实现改SQL文本 | 18/36 | 16/30 |
| 实际执行本episode新SQL | 10/36 | 11/30 |
| 原正确build / 固定提交 | 0/36、0/36 | 0/30、0/30 |
| 复核读固定code / result | 12/36、15/36 | 4/30、14/30 |
| 复核读适用audit_basis | 0/36 | 3/30 |
| 原有据复核 | 0/36 | 0/30 |

12个训练chain出现14个去重的具体义务恢复，3个初始评价chain有3个；同一修复覆盖多条旧失败时不重复计数。恢复要求后续实际请求含原错误、同义务的真实adoption/代码修改，以及确切版本随后实际执行。不能把这些局部修复算成正确业务成果；完整链原正确提交/复核分项仍为0。

当窗支持逐个保存：9个已采训练窗×4个ξ=36个独立情境块，对应54个成员块，每块M=4。仅9个provider短handoff块语义支持非空，唯一类均为proactive_handoff、b=1；逐窗n依MC0两窗3/4、RTG0两窗4/4、MC1首窗4、RTG1两窗4/4、MC2首窗3、RTG2首窗3。其余45块（实现9、复核9、chain成员27）n_positive=0、b为空。因此多方法支持块为0/54，没有组成自由度，不开展O4。这个数量是逐窗诊断块的计数，不跨θ合并组成；6个语义支持非空窗口随后概率门失败，不能把9块当作9次参数更新。provider短交接更不替代完整团队支持。原训练144条episode的V=true/false/unknown为33/111/0；活跃成员投影为216个member-slot，不增加独立episode数。

## 数值失效的有界修订

[固定六序列诊断](v014-numeric-diagnostic.json)在各失败窗真实shared-before actor张量上，逐字保留input/output/temperature，用HF缓存准备与更新方法teacher force原输出。六条high缓存重演和原保存采样概率的最大差全部为0，high整序列再现原失败；排除了此处单纯日志错配的解释。

将同一张量与序列改为highest、禁用matmul TF32后，缓存对同精度整序列最大差分别为`3.58e−6、5.87e−5、3.70e−5、2.43e−5、4.21e−5、1.49e−4`，均通过原门。它支持这些失效点来自所用数值计算路径，不能证明任意未来输入都稳定。highest缓存概率属于新Γ，不回填旧high准入。诊断无新采样/世界/optimizer step；跨case以实际actor SHA为凭证，不以初次诊断owner元数据替代。

新入口默认精度修为highest，显式high的旧协议字段和旧冻结结果不变。按[独立修订计划](../learning-v014-numeric-repair.md)仅运行一次四开发例highest容量窗，fresh base/LoRA，原阈值/角色/期限/学习率均不改，不重启六个正式运行或追加到成功。[最高精度独立容量](v014-capacity-highest-summary.json)正常完成：4例R为`[1,.2,0,.2]`，41次真实生成，输入245610、输出2656 token，无未知usage。行为和反向概率各41/41通过，最大差9.703636e−5，最大逐决定均差6.498381e−6，token加权平均绝对差7.917281e−7；actor/critic各1步。4份实际ledger守恒、终点/事件/episode绑定成立，冻结源前后一致。

实际记录12个梯度组、无遗漏，总范数.06203315，重构残差9.59e−9；post按固定规则选12个组/551输出token，无遗漏，ratio约[.98815,1.06513]，k3代理9.2515e−6；2656个实际反向token的clipping为0。原high四例与新highest轨迹不逐字相同，R差异、token与耗时不是纯数值profile的因果性能对照，尤其实现.2仅为读取项，不是正确SQL交付。

新容量耗时1172.642918秒，显存采样峰55452MiB。依原4窗×16例×6run×1.25公式，训练部分已估约39.0881 GPU进程小时，超过原24小时估计限额，且未含评价。故本轮不再追加完整六运行；后续须重冻可负担预算。原E1/E2仍未完成，最高精度有限通过不保证任意未来窗口通过，更不回填学习收益。

## 资源、核验与后续边界

启动时GPU0–2有其他项目，按容量使用GPU3–7并行5运行，第六固定排队；没有停止其他项目。15秒采样保留全设备/各PID/自身RSS；正式自身显存峰达到66310MiB，超过四例容量的55800MiB，尚未触发OOM。采样可能漏峰；共享服务器不等于同卡竞争，进程并行不证明因果性能损失。[资源JSON](v014-resources.json)显示每个运行均无同一设备上其他PID的采样记录；原五并行run的前39个资源样本在其他设备观察到另一项目。它证明当时的资源安排记录，不能排除共享CPU/I/O影响或漏采的短时竞争。共享基座文件只读，更新保存在本项目LoRA/检查点。

| 阶段 | 新闭合episode | 实际actor更新 | 进程时长总和（秒） |
|---|---:|---:|---:|
| E0三臂 | 18 | 0 | 1113.208708 |
| high独立容量 | 4 | 1 | 502.892894 |
| 原正式六运行 | 264 | 3 | 22728.178140 |
| 固定六序列数值诊断 | 0 | 0 | 152.493746 |
| highest独立容量 | 4 | 1 | 1172.642918 |
| 合计 | 290 | 5 | 25669.416405 |

总计约7.1304 GPU进程小时，包含世界执行、I/O、模型加载及检查，不是纯GPU内核计算时间。正式六运行调度墙钟5840.414339秒（约1.6223小时），并行时长不可直接相加作为墙钟。诊断teacher forcing无新episode；容量更新不计入原正式3次更新。

[实际存储](v014-observed-storage.json)按结束后的原实验输出目录`du -sb`统计为13668893323字节（约13.67GB），不含基座、冻结源码、资源日志、CPU机制产物和派生报告。与[事前容量外推](v014-storage-capacity.json)分开，既未把未执行744例的完整估计当实际，也未假称设置了文件系统硬配额。

定向CPU检查覆盖实际世界路径、ledger、呈现来源、toy训练信号、评测guard及只读汇总。新增只读报告fixture共13项通过（signal 2、study 4、work 7），另对4条既有真实记录作只读断言；受影响的训练/评价toy检查与机制实验见各自JSON及设计文档。最终 `ruff check src tests scripts` 与 `git diff --check` 通过。没有重跑旧842完整回归，不以测试数作为学习阶段终点。v0.13时长原始和3021.0102248191833秒，四舍五入3021.01；逐行先两位再加为3021.02，属汇总舍入差。

本轮只有一个构造SQL家族；内部新事实、交换持有者和规则锁定不等于独立来源。真实不可得的合法阻塞、新搭档、独立实际项目和四项目迁移未验证。后续先按新精度实际代价重新冻结有限学习研究，不把数值修复或短窗口更新当作学习收益；有当前多方法支持后才进行同预算ID-VTDO配置比较。
