# v0.10：评价边界、工作人员运行与场景模块计划

日期：2026-09-24。依据：[v0.9审计](reference/platform-modules-audit.md)。起始基线`7a622539b1d57612ddc56923cb8ca21a4344b246`。保留两模板、工作绑定、发布维护及共同问题关系，不增加行业、自由专业正文评价或模型/训练实验。

## 阶段A：R0与R1

| 工作 | 合同与验收 |
| --- | --- |
| R0评价输入边界 | 完整旧源的真实会话合法采用/读取/写入/提交；null目标、null结果行与正常/业务错误对照；显式结构校验，不catch-all归成员工算错 |
| 评价分类 | pass、structure_failure、content_failure、source_unavailable、unassessed、evaluator_error；独立目标未评保持未评；注入真实实现异常验证故障归因 |
| 统一运行模块 | 每个角色拥有独立memory/上次真实返回/绑定身份；轮转提供一次机会，策略决定动作，运行器最多执行一次公开工具调用 |
| 规则策略接入 | 保留现有业务算法，作者/审阅者独立策略决定；正确首交、两项问题分次处理、错误意见反驳及财务核对，主驱动不传findings或正确正文 |
| R1语义比较 | 与旧受控公开驱动比较固定源、真实提交/问题/回应/决定、结果及历史；不要求动作数或日志形状相同；同时对照独立真值 |

阶段A冻结后执行正式R0/R1及必要旧内容/角色回归。场景与后续评价接口可以并行准备，但不将未执行的阶段B能力算入阶段A成果。

## 阶段B：R2–R4

| 工作 | 合同与验收 |
| --- | --- |
| R2独立角色 | 作者和审阅者只有自身公开观察与记忆；等待一人不挡其他人；显式故障策略的错误动作产生真实后果，调度器不代修 |
| R2范围 | 旧意见/旧回复隔离；私有资料和独立评价答案不进入无权角色输入；完整动作返回后checkpoint续行，不承诺任意策略崩溃恢复 |
| R3场景构建 | 单模板与联动场景经统一规格部署，项目/材料/角色/共享/事件/边界明示；部署和外部事件均通过真实工具，无私改状态 |
| 结构对照 | 同规格业务起点一致；一次只改direct→route或更正发生阶段；表面命名变化单列，不混作不同流程 |
| 起点 | initial只装背景材料；waiting/pending/partial起点必须由工作人员真实前缀产生并单列记录，不伪造旧审批/阅读历史 |
| 不可构建 | 缺资料、矛盾引用/规则或前缀不可达有明确诊断，不降级更容易情境；事件触发和完成/预算边界不能看评分临时改动 |
| R4经历与评价 | 实际tools/observation/action/response、环境事件、控制器干预分开保存；独立端口捕获逐项核对；固定提交重复评价一致且世界事实不变 |
| 分项总结 | 制度进度、有限内容、过程约束、不完备、运行问题分别呈现；上游错而下游忠实、metadata对而正文错、合理未知、评价器故障各有明确例证 |

## 分层与接口

工作人员策略`decide(context)`接收自身公开tools、observation、memory和上次action/result；返回act/wait/done与更新后的自身memory。StaffRuntime不导入领域匹配、撰写或审阅算法，不替策略安排正确步骤。策略可被替换，当前只运行透明规则和明确标记的故障策略。

Scenario声明世界、项目、角色、安装者、setup、events、start及boundary。有限宿主可按声明条件读取必要状态执行外部事件，但不读取独立评价来救场。原始事件与背景事实、真实前缀、正式episode分别记录。Episode结束是运行边界，不删除世界或其他项目。

ExperienceRecorder保存发生时的真实返回；assessment为只读旁路，不送入策略context。新运行和场景格式独立版本化；若未新增核心事实，不仅因软件版本变化强制更换世界状态格式。旧归档不覆盖。

## 执行与记录原则

每阶段先保留实际反例及开发失败，冻结协议与分母后记录源码/脚本/测试起止身份。不同世界并行，单世界单写者。测试按改动范围执行；正式冻结后运行必要矩阵与完整回归，不机械扩展旧故障矩阵。

本轮不自动启动API/GPU实验。后续单角色固定模型试跑须与规则结果分开，失败不能用规则策略代做并算模型成功。真实资料用于未来校准，未知历史不补造。完成后写详细实验说明并自动提交推送。


## 阶段A冻结前收口

R0协议14条件×7检查=98：正常、完整业务错、若干明确结构错、来源读取不可用、无目标合同未评、两类真实内部异常注入。R1为4场景40项，统一角色各自公开观察/行动，与原受控驱动语义结果及独立真值比较；动作数/日志形状不作为同解要求。

R1最初脚本导入失败保留；分拆交付第二文件加入后策略重新选输出别名产生歧义，保留真实公开世界并以精确work memory记住已合法选定别名修复，不改内核。阶段A字节保持命题限定初始输入/背景版，不把未做的首交错误版本对照倒填。

内容评价未声明content_checks时明确unassessed，旧M4仅required_fields完整的预期随新接口修订为字段齐全且内容未评，原JSON不改。场景模块和部分R2策略已作为并行准备落盘；阶段A正式结果不据此宣称R2–R4已验收。
