# v0.14 概率门停止后的有界数值修订

正式冻结`0dda1bf`的六个high运行均按原阈值停止，不能继续将它们计作完整MC/RTG学习对照。原264条真实经历、3次更新与480个未启动槽永久保留。没有放宽max0.02/mean0.002，不换失败seed，不在原run补采或恢复。

六个失败窗各取原最大绝对概率差的一条序列（只按数值失败选择，不看reward），恢复该窗shared-before的实际actor张量；比较high和highest各自的缓存teacher forcing与整序列重算。原input/output token全部保留，没有新模型采样、世界交互或optimizer step。固定选择见numeric-failure-diagnostic.json。highest是新的数值协议；它的重算不能冒充原high采样概率、不能修复旧准入。代码同时检查high缓存重演是否复现原记录。此选择只诊断确认的失效点，不能估计总体错误率。

当前初步数值证据支持将新入口默认matmul从high改为highest；明确指定high的旧协议仍按自身字段执行，旧冻结代码和结果不变。默认变更也属于Γ变化，不能直接跨两种profile恢复训练状态或混合支持池。

后续仅作**一次四开发案例的独立highest容量窗**，使用原capacity四任务、固定采样seed及原期限，fresh base/LoRA；维持原概率阈值、一个更新机会与完整诊断。无论通过或失败都不补采，它检验真实自回归采样/整序列/反向概率一致性和新profile代价，不以两次更新或成功率宣称学习阶段完成。原E1/E2的三seed四窗比较仍记为未完成，不重启六运行来消除失败。本轮在保存实际修复证据与更新后续资源估计后结束，新的完整学习研究须重新冻结计划与所有成本。

该修正不改任务、界面、奖励、角色、动作、学习率或支持门槛。若highest仍失败，保留失败，不临时删token或回填概率。新来源泛化、ID-VTDO配置收益和四项目迁移仍不据这项诊断建立。
