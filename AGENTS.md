# 项目协作指令

减少冗余校验，只做必要核验。工作完成后自动进行 git 提交与推送远端，用户已授权推送操作，无需额外请求。每次较大的改动写好说明文档。实验文档尽可能详细，客观表述实验结果，猜测类内容应说明清楚，以免造成误解。

项目可以使用 API、GPU 等资源，实验数据可以发送至 DeepSeek，无需单独请求。优先考虑新版设计实现，不要为了旧版兼容导致实现偏差。`DEEPSEEK_API_KEY` 在 `.env` 文件中；勿将密钥文件纳入提交。

实验与修订优先考虑并行处理，提升效率，充分利用服务器 CPU 与 GPU 资源。

本项目的测试命令为 `.venv/bin/python -m pytest -q`，静态检查为 `.venv/bin/python -m ruff check src tests scripts`。机制验证与真实模型运行方法见 README；详细实验记录保存在 `docs/experiments/`。规则基线、真实模型实验和参数训练收益必须分别表述。
