# 实际交付样例

这些文件来自 2026-09-22 的真实 DeepSeek `deepseek-flash` 试跑：seed=101、continuous、clarification，第二轮 `work-2` 的实际提交。

- `operating_model.xlsx`：包含 Inputs、Outputs 和 Sensitivity，保留真实公式和计算缓存。
- `research_memo.json`：与模型和修订披露绑定版本的研究备忘录。
- `financials.json`：该轮指定的合成披露。
- `manifest.json`：原始 artifact/version/submission ID 与文件 SHA-256。

材料是虚构项目数据，不是真实企业披露或内部文件。以上只是交付物样例，不包含完整世界；创建可继续运行的世界请使用 README 的 build 命令。完整原始运行和导出包保存在本地 `runs/live-v0.1/`、`runs/exports-v0.1/`，未纳入 Git。
