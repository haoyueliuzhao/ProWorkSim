# 核心行动与提交合同（v0.5）

身份由可信会话绑定；能力来自组织配置。执行合法性、制度合规性、独立质量分别判断，允许可执行的业务错误留下真实后果。

| 行动 | 执行依据与效果 | 必须保持 |
| --- | --- | --- |
| Read | 真实版本可访问，返回实际内容并记录读取 | 不自动确认、采用或验证 |
| CreateVersion / DeclareAdoption | 对象写权和合法引用；创建真实不可变版本并声明来源 | 原版本字节与历史采用关系不改写；错误内容仍可落盘 |
| Confirm | 对主题与工作范围有确认权；签发明确凭据版本 | 不把假设变事实，不更新下游成果 |
| Request | 当前义务、真实接收者和绑定条件 | 发信本身不满足条件 |
| Reply | 保存真实送达与原始接收事实，重新判断满足哪些条件 | 不相关工作、其他版本权限与历史不变；旧／错／不可得回复不伪装满足 |
| Submit | 纯工作视图允许且身份是 owner，固定交付版本 | 不自动批准，也不保证结果正确 |
| Withdraw | 当前 owner 的真实 pending 提交；可与前驱等待独立 | 保留版本、撤回事实和原观察，不强制改文件 |
| Approve | 当前前驱、条件和提交版本满足，身份有批准权 | 错误成果也可能被有权批准，Q 可独立拒绝 |
| ReviseRequirement | 有范围的修订权，显式作用对象 | 产生替代义务、保留历史；不自动修正文档 |
| RestoreInformation | 有权环境行动，真实且同需求适用的证据 | 仅恢复选中条件的获取机会；仍需新实际回应来满足 |

## 精确回执

成功业务转换记录 `apply_delta`、`derive_delta`、`net_delta` 三个实际阶段差分，以及 `primary_region_changes`、`projection_region_changes` 两类区域净变化。前置的缓存重建另记 preflight，不混入 Apply。

未完成阶段不冒充空差分：以 null / failed_phase 表明阶段没有完成端点，attempted_delta 记录异常前尝试的状态变化。回滚后的净效果可以为空。差分为路径和摘要，不是完整值 patch。

运行器随后增加逻辑时间、尝试记录、排程和提交登记；这些记账不包含在业务 after_derive 快照中。旧回执不转换为伪造的新轨迹。

## 独立提交与恢复

每个有限 JSON 命令及环境事件具有独立身份、绑定主体、完整请求摘要、前置 revision、语义版本、提交 revision 与结果。正式命令结果存于 `operation_commits`；同标识重试不再执行原效果。身份冲突与非法非有限负载在执行前拒绝，不推进世界。

一个已进入协议的工具错误可以形成已提交的拒绝尝试，逻辑时间仍可推进；到期事件随后单独处理。命令已提交但事件尚未提交时，命令仍为成功，未完成事件保留且明确报告。事件已提交后仅交付确认失败时，不得恢复旧文件前缀；记录 event_delivery_errors，重试不能重复正式效果。

暂存文件不是正式版本；只有原子状态指针与登记共同提交后才成立。恢复验证已提交文件，隔离孤儿暂存，修复物化镜像，并处理仍到期的事件。它不重新采样员工策略、不修正错误公式。

保证范围限定为单世界、单写者、现有文件系统与声明工具／切点。JSON 原子替换及文件 fsync 不等于已证明断电持久性；本轮不作该声明。
