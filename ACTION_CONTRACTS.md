# 核心行动与提交合同（v0.7）

身份由可信会话绑定；能力来自组织配置。执行合法性、制度合规性、独立质量分别判断，允许可执行的业务错误留下真实后果。

| 行动 | 执行依据与效果 | 必须保持 |
| --- | --- | --- |
| Read | 真实版本可访问，返回实际内容并记录读取 | 不自动确认、采用或验证 |
| CreateVersion / DeclareAdoption | 对象写权和合法引用；创建真实不可变版本并声明来源 | 原版本字节与历史采用关系不改写；错误内容仍可落盘 |
| Confirm | 对主题与工作范围有确认权；签发明确凭据版本 | 不把假设变事实，不更新下游成果 |
| Request | 当前义务、真实接收者和绑定条件 | 发信本身不满足条件 |
| Reply | 保存真实送达与原始接收事实，重新判断满足哪些条件 | 不相关工作、其他版本权限与历史不变；旧／错／不可得回复不伪装满足 |
| Submit | 纯工作视图允许、身份是 owner、声明凭据当前适用；固定实际交付版本。WorldCore 动态合同允许一份或多份产物 | review 政策等待有权审阅；delivery_only 记录 owner 的交付完成依据，不虚构经理批准；均不保证内容正确 |
| Withdraw | 当前 owner 的真实 pending 提交；可与前驱等待独立 | 保留版本、撤回事实和原观察，不强制改文件 |
| Approve | 当前前驱、条件和提交版本满足，身份有批准权 | 错误成果也可能被有权批准，Q 可独立拒绝 |
| ReviseRequirement | 有范围的修订权，显式作用对象 | 产生替代义务、保留历史；不自动修正文档 |
| RestoreInformation | 有权环境行动，真实且同需求适用的证据 | 仅恢复选中条件的获取机会；仍需新实际回应来满足 |

## 世界、项目和动态对象

| 行动 | 合同与边界 |
| --- | --- |
| InstallProject | 世界装载权；纯预检后同一提交安装项目、对象、工作与局部授权，文件写入失败也整体回滚 |
| CreateObject | 项目创建权或世界创建权；运行中注册启用能力的 JSON/XLSX 对象与首版，路径受控，不覆盖已有身份 |
| Share | 所有者空间和共享权；精确版、目标项目、已登记主体，follow_updates 必须显式选择。世界操作者可预先分享给未来项目，装载再核对参与关系 |
| Adopt / AdoptVersion | 可读精确版本、项目采用权；current 政策允许显式更新并保存 history，fixed 政策通过新声明改变；不自动改输出 |
| CloseProject | 完成需当前义务均 accepted；归档按 pending_obligations=retain/cancel 决定待办去向。retain 仍接收待到回复，cancel 记录取消后果，其他项目保持 |
| Start/EndEpisode | 主体在所选项目中的边界记录；不改变工作、共享或世界生存状态 |
| Pause/Resume | 世界明确权限；暂停准入和环境执行，恢复继续到期事件，不清空队列 |

独立内容检查读取固定 JSON/XLSX，验证声明的字段、标量、公式缓存及精确来源关系；不支持的规则在包准入时拒绝，不将未实施规则算作通过。工作人员可以提交字段不足的合法对象；制度历史与独立失败结果分开。

## 精确回执

成功业务转换记录 `apply_delta`、`derive_delta`、`net_delta` 三个实际阶段差分，以及 `primary_region_changes`、`projection_region_changes` 两类区域净变化。前置的缓存重建另记 preflight，不混入 Apply。

未完成阶段不冒充空差分：以 null / failed_phase 表明阶段没有完成端点，attempted_delta 记录异常前尝试的状态变化。回滚后的净效果可以为空。差分为路径和摘要，不是完整值 patch。

运行器随后增加逻辑时间、尝试记录、排程和提交登记；这些记账不包含在业务 after_derive 快照中。旧回执不转换为伪造的新轨迹。

## 独立提交与恢复

每个有限 JSON 命令及环境事件具有独立身份、绑定主体、完整请求摘要、前置 revision、语义版本、提交 revision 与结果。正式命令结果存于 `operation_commits`；同标识重试不再执行原效果。WorldCore 身份包括 actor/project/request_key，跨项目同文本键互不吞并；世界操作使用独立上下文。身份冲突与非法非有限负载在执行前拒绝，不推进世界。

一个已进入协议的工具错误可以形成已提交的拒绝尝试，逻辑时间仍可推进；到期事件随后单独处理。命令已提交但事件尚未提交时，命令仍为成功，未完成事件保留且明确报告。事件已提交后仅交付确认失败时，不得恢复旧文件前缀；记录 event_delivery_errors，重试不能重复正式效果。

暂存文件不是正式版本；只有原子状态指针与登记共同提交后才成立。恢复验证已提交文件，隔离孤儿暂存，修复物化镜像，并处理仍到期的事件。它不重新采样员工策略、不修正错误公式。

保证范围限定为单世界、单写者、现有文件系统与声明工具／切点。JSON 原子替换及文件 fsync 不等于已证明断电持久性；本轮不作该声明。

## v0.7 工作能力与发布动作

| 行动 | 合同 |
| --- | --- |
| CreateObject / SheetUpdate / SheetRecalculate | 启用相应能力、合法别名与写 ACL；实际 JSON/XLSX 字节进入同一不可变版本体系，错误公式可保存 |
| Share | 基础 ACL 与精确分享共同生效；同项目分享不再被提前返回忽略，未分享的人/版本仍拒绝 |
| Adopt / AdoptVersion | 从可信项目内解析实际对象和每个目标工作，再检查窄授权；fixed 不能直接换版 |
| Publish | 已有精确版本、写 ACL 和独立发布权；保存范围内的 release，只沿明示订阅产生分享和通知 |
| RequestInformation | 只用公开 route_id/work_id 选择预声明材料路线；后续真实事件才送达或报告 unavailable，并按路线精确开放材料 |
| ContentEvaluate | 固定提交字节、采用快照、明确标量或消费接口；来源贡献文件须有精确依赖；不读取未需要的私有上游 |

显式政策下，草稿写入不要求额外发布权，也不产生正式发布。旧 implicit_write 保留其写后发布政策约束。发布、采用和内容成功分别报告，不能因合法批准或元数据 current 而跳过独立检查。
