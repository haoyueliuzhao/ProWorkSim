# 核心行动合同（v0.4）

身份由可信会话绑定。下表是实际共享函数的合同；经营工具与文稿工具可有不同参数展示，但不得改变共同制度语义。

| 行动 | 执行前置条件 | 直接效果 | 派生与保持 |
| --- | --- | --- | --- |
| Read | 引用存在、时间已到、具体版本可访问 | 返回实际字节／内容，记录提供或读取 | 不自动声明采用、不授予新版本权限、不确认正确 |
| CreateVersion | 对该对象有写权；来源引用可访问 | 新不可变文件版本及声明来源 | 旧字节保持，依赖状态可变，下游字节不自动改 |
| DeclareAdoption | 明确目标版本与来源版本 | 写入版本时声明来源关系 | 不等于使用了正确单元格，独立评价可拒绝 |
| Request | 工作真实且当前；收件人存在；附件可发送 | 消息与版本化请求 | 不凭发信本身满足条件 |
| Reply | 请求真实；身份与消息发送条件成立 | 保存送达、关联请求，必要时授权明确版本 | 逐条件判断；旧／错角色／不可得不伪装满足；无关阻塞和文件保持；重复回复幂等 |
| Confirm | 组织规则允许该身份对该主题和范围确认 | 凭据版本及正式 attestation | 不把假设变事实，不自动更新依赖成果 |
| Submit | 工作允许提交、前置义务满足、引用合法 | 固定当前交付版本 | 可提交业务错误；不能伪装批准 |
| Withdraw | 本人当前待审提交 | 记录撤回、保留版本 | 旧审阅事件不能批准；允许原文件重交 |
| ReviseRequirement | 组织具有范围内修订权 | 显式 targets 的新需求版本与替代义务 | 历史事实保持；待审旧提交失效；不修正任何工作人员字节 |
| Approve | 身份有批准权、提交未失效且版本符合制度要求 | 明确批准该提交 | 不调用独立评分器，不保证专业或数值正确 |

## 运行器责任

- 在执行前由适配器生成允许影响的状态路径；转换检查比较前后状态并拒绝越出范围的业务改动，记录 Apply 与 Derive 的实际效果。
- 外部文件写入失败通过已有存储恢复机制恢复提交指针和可见文件；无引用的写入临时版本不当作已提交事实。
- 时钟／尝试日志不计为业务副作用；随后到期事件具有自己的效果记录。失败动作仍可推进时间并触发合法事件。
- 对独立对象的动作比较业务状态而非字面日志顺序；对同一对象的冲突采用串行顺序，避免声称并发交换一致。
- 规则检查使用四态结果；原经营评价二值检查和未评数组可作为输出适配，未评不能被折算为成功。

## 制度权限与质量的界线

无权限身份写“批准”两个字不会产生正式效力。有权限的审阅者可以批准错误成果，世界应保留这个错误组织决定；独立评价另行拒绝。缺当前凭据时不得借用隐藏假设生成本应未知的正确目标。实验须同时覆盖可执行错误、拒绝执行、合理阻塞和合法完成。
