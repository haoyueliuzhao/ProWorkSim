# 工作世界语义内核（v0.4，有限范围）

依据：[本轮审计](docs/reference/semantics-kernel-audit.md)。本规格规定正在实现的窄内核，不宣称通用组织模拟或领域真实性。

## 分层

`core/` 定义引用、版本可见性、凭据适用关系、条件满足、工作替代和行动保持条件。`policies/organization.py` 配置身份与制度权力。`domains/` 定义经营计算或文稿内容合同。`adapters/` 将实际文件、表格和通信接入这些机制。工作人员策略和世界外独立评价不决定内核写入许可。

同一权限主体可以写错公式、错误声明采用或过早提交；有权审阅者可以批准数值错误的文件。身份／版本／引用错误可以使行动不能执行；业务不合适与结果错误不自动变成工具执行错误。

## 基本对象与关系

| 对象／关系 | 含义与边界 |
| --- | --- |
| Actor | 环境绑定身份；可重命名，其权力来自组织配置 |
| VersionRef | 明确对象和不可变版本；“最新”只是一项指针 |
| Fact / Claim / Assumption / Credential | 来源记载、主体主张、条件假设、正式凭据分别标注；批准不使假设变成事实 |
| Requirement / Work | 某版本的义务、用途、作用范围与交付要求；工作图只表达部分前置约束 |
| Attestation | 有权主体的正式确认记录，绑定凭据版本与范围 |
| Request / Response / Condition | 请求、送达回复与条件是否满足分别保存 |
| Submission / Review | 提交固定文件版本；审阅的制度决定与外部质量判定分开 |
| Access / Observation / Adoption / Support | 可访问、实际提供或读取、声明采用、验证支持分开；不据此推断模型相信什么 |

`Applicable(credential_version, requirement_version, dimension, work_node, purpose, period, time)` 是关系查询，不是全局“最新版本”比较。同一凭据可以适用工作 A、不适用工作 B。界面摘要可折叠为 current/stale/unknown，但必须保留每工作关系，不能选择一个工作掩盖冲突。历史已批准提交按其固定版本解释，不用今天的文件改写历史质量。

## 状态演化

动作按 `Apply → Derive → Commit` 执行。Apply 经适配器产生真实文件或业务记录；Derive 只更新可重算关系，不修改文件内容；Commit 检查声明作用域与历史保持。失败的直接变更回滚；尝试日志、逻辑时钟及到期事件单独处理，因此失败动作不等于世界全不变。

每个行动声明允许修改的业务对象路径，额外记录实际变更路径。时钟、日志、到期事件及派生状态单独标识。旧不可变文件的身份、来源和哈希不得修改；旧提交的内容与已完成审阅不可重写。事件在相同到期时刻按入队顺序串行处理，不宣称任意并发事务的交换律。

CheckStatus 为 PASS、FAIL、UNASSESSED、NOT_APPLICABLE。缺适用凭据的目标不可评，不用隐藏生成值补足；如果任务明确允许条件化假设，应由其公开合同声明，不能由评分器临时允许。

## 回复与恢复

回复送达后才查询其满足哪些条件：请求、工作与需求版本、提供者、用途、证据版本及制度权限必须满足相应合同。收到 unavailable 只表示当前回应不能提供材料，不等于条件满足，也不等于永久不可得。重复处理同一回复不得追加正式效果。明确存在后续可用事件或重试机会时保持可恢复等待；无剩余机会时可结束当前 episode 为 blocked_unavailable，不能计业务完成。

## 本轮复用边界

保留经营分析主模板，增加一个最小文稿审阅发布模板，复用同一版本、授权、凭据、回复判断、提交／撤回／修订函数和转换检查。第二模板只检验抽象是否摆脱具体经营字段，不作为第二行业基准。部分旧经营编译、工具展示和专业审阅仍属于适配代码；目录重命名不作为内核复用证据，报告需列明实际共用调用路径。
