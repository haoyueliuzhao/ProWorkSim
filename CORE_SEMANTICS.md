# 工作世界语义内核（v0.5，有限状态一致性与恢复）

依据：[本轮审计](docs/reference/state-consistency-audit.md)。本轮保留经营世界与文稿微模板，优先统一事实解释、行动使能和提交结果，不增加行业、模型评测或训练。

## 对象与分层

`core/` 定义版本、组织效力、上下文适用性、条件、工作与转换；`policies/` 定义静态权力；`domains/` 定义有限业务内容；`adapters/` 接入真实文件和通信。独立评价 Q 不反向决定世界事实。有权主体可以批准错误成果，重算不能撤掉这一历史决定。

Fact、Claim、Assumption、Credential 分别表示来源记载、主张、条件假设和制度凭据。确认不使假设成为事实，声明采用不证明计算正确。可访问、实际观察、声明采用和验证支持分别保留，不推断模型内部相信什么。

适用性始终是明确版本相对于工作、需求维度／版本、用途、期间与时间的关系；当前版本指针不是全局效力。共用产物对不同工作可以有不同结果，界面摘要不得掩盖这些关系。

## 四类状态

| 部分 | 基础内容与职责 |
| --- | --- |
| B：基础事实 | 不可变文件版本、正式签发、工作定义与替代、提交、审阅／撤回／失效、编辑事件、条件定义与绑定／接收／撤除事实 |
| P：派生视图 | condition/work view、兼容 blocker、按工作适用性、公开使能与摘要 |
| Q：运行进度 | 事件队列、已提交操作登记、revision、失败事件尝试和恢复进度 |
| O：实际观察 | 原始消息、工具结果、读取收据与模型运行记录；后续投影不改旧观察 |

从当前格式的可信检查点读取 B、时钟和固定政策，重新计算两个指定视图。`derive_condition_view` 只折叠条件事实和原始回应；`derive_current_work_view` 根据当前实例、制度提交事实、条件与前驱计算状态和使能。不会读取旧 status 或 blocker 作为权威。

回应接收时固定当时的需求、替代关系、正式凭据、组织配置和证据定义。这些是事实快照，不是保存 PASS/FAIL 答案；重算仍执行匹配，避免后来的同 tick 签发或需求变化倒改过去回应的含义。

待审提交、前驱就绪、条件未满足和业务摘要是不同维度。前驱等待不抹掉 pending；本人可以撤回当前待审提交。批准需满足当前前驱、条件以及版本要求；公开 enabled_actions 还按观察者身份过滤。

## 转换与提交

查询和执行前可在内存重建缓存；其差分单独记为 `preflight_projection_delta`。业务转换保留三个实际阶段边界：

- `apply_delta = diff(before, after_apply)`。
- `derive_delta = diff(after_apply, after_derive)`。
- `net_delta = diff(before, after_derive)`。

字段所属区域另记 `primary_region_changes` / `projection_region_changes`，不把区域误当执行阶段。未完成阶段使用 null 与 failed_phase，并保留 attempted_delta；回滚后的净业务效果为零，不代表未曾尝试修改。

这些差分是路径列表和摘要，不是完整的值补丁。时钟、交互日志、事件排程与 journal 的运行器记账在业务阶段之后；after_derive 的摘要不是最终 state.json 摘要。旧 v0.4 回执保留原语义，不伪装成新阶段轨迹。

动作和每个到期环境事件分别原子保存状态与操作登记。命令成功提交后，事件仍可能失败；返回明确区分 `pending_event_errors` 和已提交事件的 `event_delivery_errors`。保存成功后绝不按旧前缀回滚文件。

## 有界恢复与范围

操作身份在单世界、绑定身份空间内定义；摘要覆盖有限 JSON 的完整 action/arguments。相同身份同负载返回原命令结果，不重复正式效果；不同负载显式冲突。非法非有限 JSON 在身份建立前拒绝，不用截断 repr 伪造摘要。

不可变版本先写，原子状态快照决定其是否成为正式事实。恢复检查提交前缀和版本哈希、重建当前镜像、隔离未被正式状态引用的暂存版本；不重算或修正错误文件、不重新调用模型。未完成事件保留在队列；已提交事件重放只确认队列，不再次批准或授权。

运行器只接受当前 v0.5 schema，不隐式把旧状态缓存解释成新事实。旧实验使用其冻结运行器；本轮不提供全量历史迁移。

本轮不实现任意日志溯源、跨机器共识、断电持久性或任意插件事务。支持范围由两个模板、明确缓存字段、深度边界和故障切点限定。专业外部合理性需要另行校准，内核测试不替代它。
