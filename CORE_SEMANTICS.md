# 工作世界语义内核（v0.9，两模板与共同审阅关系）

依据：[两模板审计](docs/reference/template-expansion-audit.md)。保留多项目WorldCore、工作采用、发布和持续义务；本轮用财务集合核对与报告定向审阅验证共同规则，新增定位问题、回应和处理决定，并明确工具拒绝归因。领域差异留在各自合同与程序策略中，见[设计说明](docs/template-expansion-v09.md)。

## 世界先于项目

`core.world.WorldSpec` 只声明世界身份、主体、应用和环境政策；不要求项目、金融事实或假设。`ProjectPackage` 将项目参与关系、材料、义务、合同和局部授权原子装入同一个状态与对象注册表，不为每个项目另建运行器或世界目录。旧单项目规格明确称为 `OperatingTemplateSpec`。

世界的运行／暂停、项目的 active/completed/archived、工作义务的生命周期和 episode 的选取边界分别记录。项目完成不结束世界；归档不删除人员、文件或正式历史；episode 结束不清空别的项目。无活动项目时世界可以 idle，之后继续装载项目。暂停限制新工作和环境事件处理，读取／拒绝仍可产生审计逻辑计数。

世界主体身份稳定；项目岗位不产生隐式权力。工作 ID 全局限定，项目内别名和文件显示名不充当对象身份。同名文件写到分离的受控镜像路径。可信会话绑定 actor/project，工具参数不能覆盖这两个字段；命令身份按 actor/project/key 分域，摘要仍覆盖完整上下文与负载。

共享针对精确对象版本、目标项目和主体。发布后续版本只沿显式 follow_updates 关系授权，不沿 derived_from 递归开放上游。当前采用政策与固定快照分开；发布可以改变关系并送达通知，不能代替工作人员编辑。实际合法阅读历史不会在项目切换时被删除。

## 对象与分层

`core/` 定义版本、组织效力、上下文适用性、条件、工作与转换；`policies/` 定义静态权力；`domains/` 定义有限业务内容；`adapters/` 接入真实文件和通信。独立评价 Q 不反向决定世界事实。有权主体可以批准错误成果，重算不能撤掉这一历史决定。

Fact、Claim、Assumption、Credential 分别表示来源记载、主张、条件假设和制度凭据。确认不使假设成为事实，声明采用不证明计算正确。可访问、实际观察、声明采用和验证支持分别保留，不推断模型内部相信什么。

适用性始终是明确版本相对于工作、需求维度／版本、用途、期间与时间的关系；当前版本指针不是全局效力。共用产物对不同工作可以有不同结果，界面摘要不得掩盖这些关系。

## 工作能力、发布与实际使用

applications 选择 files/spreadsheets 的有限工具能力。文件适配器负责真实字节与公式缓存；核心保持身份、授权、精确版本和事务，不规定金融算式。XLSX 错误可落盘/提交，评价读取固定不可变版本与明确独立标量，不借正式批准代替内容正确。

基础 ACL 和精确分享均参与读取，同项目分享也生效。初次与更新采用逐工作带上真实对象上下文；候选别名不会产生授权。发布记录与读取授权分开，current_published 查询目标项目范围内最后发布版；草稿 current_version、release、adoption 和正文实际来源分别记录。

新世界默认 explicit；只有 publish 动作创建正式发布。声明的 implicit_write 后续保存可产生发布，但不会为初始材料或旧历史补造 release。发布后仅既有显式订阅路线获得精确授权/通知，不递归开放上游。

别名指向对象，采用按 `work_id::alias` 分开保存；修订不把旧采用转给新工作。合同先声明允许政策和 fixed 版本，执行与评价核对同一声明。提交固定该工作采用快照及当时目标。独立消费评价同时检查正文引用、所贡献文件的精确依赖和接口数值；全部标签改为新版不等于实际内容正确。递归 JSON 比较区分任意深度的 bool 与 number，冲突不以文件顺序挑选赢家。A 的领域质量、B 对接口的忠实消费和总体目标分别评价。

项目维护规则显式选择来源、工作谱系、事实阶段、后果及授权主体。发布时捕获具体工作/需求/阶段，随后单独提交通知、修订、accepted 后继或忽略事件；重复事件不重复义务。维护后继保留已接受前项，以独立 ID 和新需求继续；正式修订则沿既有替代语义处理旧项。后台不更新交付文件或替工作人员采用。

资料路线可跨同一工作谱系复用，但实际请求冻结具体工作、需求及证据。迟到旧回复不满足新工作。提供者恢复可得性后，工作人员重新请求；新路线修订下的请求取代旧未满足条件，原始回应保留。

公开程序工作人员只持有 tools/observe/call。ContinuousWorker 在多个绑定端口与当前任务间轮转，每步最多一个工具动作；实际观察和往返在发生时保存。策略等待、内核条件阻塞、预算截断、环境错误分别记录。策略 checkpoint 只承诺已完成步骤之间的显式恢复，不等同任意进程中断的恰好一次执行。该有限程序策略不代表一般规划或模型能力。

## 四类状态

| 部分 | 基础内容与职责 |
| --- | --- |
| B：基础事实 | 不可变文件版本、正式签发、工作定义与替代、提交、审阅／撤回／失效、编辑事件、条件定义与绑定／接收／撤除事实 |
| P：派生视图 | condition/work view、兼容 blocker、按工作适用性、公开使能与摘要 |
| Q：运行进度 | 事件队列、已提交操作登记、revision、失败事件尝试和恢复进度 |
| O：实际观察 | 原始消息、工具结果、读取收据与模型运行记录；后续投影不改旧观察 |

从当前格式的可信检查点读取 B、时钟和固定政策，重新计算两个指定视图。`derive_condition_view` 只折叠条件事实和原始回应；`derive_current_work_view` 根据当前实例、制度提交事实、条件与前驱计算状态和使能。不会读取旧 status 或 blocker 作为权威。

回应接收时固定当时的需求、替代关系、正式凭据、组织配置和证据定义。这些是事实快照，不是保存 PASS/FAIL 答案；重算仍执行匹配，避免后来的同 tick 签发或需求变化倒改过去回应的含义。

待审提交、前驱就绪、条件未满足和业务摘要是不同维度。前驱等待不抹掉 pending；本人可以撤回当前待审提交。批准需满足当前前驱、条件以及版本要求；公开 enabled_actions 还按观察者身份过滤。

## 转换与提交

查询和执行前可在内存重建缓存；其差分单独记为 `preflight_projection_delta`。业务转换保留三个实际阶段边界：

- `apply_delta = diff(before, after_apply)`。
- `derive_delta = diff(after_apply, after_derive)`。
- `net_delta = diff(before, after_derive)`。

字段所属区域另记 `primary_region_changes` / `projection_region_changes`，不把区域误当执行阶段。未完成阶段使用 null 与 failed_phase，并保留 attempted_delta；回滚后的净业务效果为零，不代表未曾尝试修改。

这些差分是路径列表和摘要，不是完整的值补丁。时钟、交互日志、事件排程与 journal 的运行器记账在业务阶段之后；after_derive 的摘要不是最终 state.json 摘要。旧 v0.4 回执保留原语义，不伪装成新阶段轨迹。

动作和每个到期环境事件分别原子保存状态与操作登记。命令成功提交后，事件仍可能失败；返回明确区分 `pending_event_errors` 和已提交事件的 `event_delivery_errors`。保存成功后绝不按旧前缀回滚文件。

## 有界恢复与范围

操作身份在单世界、绑定身份空间内定义；摘要覆盖有限 JSON 的完整 action/arguments。相同身份同负载返回原命令结果，不重复正式效果；不同负载显式冲突。非法非有限 JSON 在身份建立前拒绝，不用截断 repr 伪造摘要。

不可变版本先写，原子状态快照决定其是否成为正式事实。恢复检查提交前缀和版本哈希、重建当前镜像、隔离未被正式状态引用的暂存版本；不重算或修正错误文件、不重新调用模型。未完成事件保留在队列；已提交事件重放只确认队列，不再次批准或授权。

WorldCore 接受 `world-core-v0.9`，经营与文稿适配器继续接受其 `0.5` 状态。两者调用相同事务运行器，格式不隐式转换；旧实验保留冻结解释，本轮不提供完整历史迁移或旧 XLSX 世界自动打包。

本轮不实现任意日志溯源、跨机器共识、断电持久性或任意插件事务。旧 v0.5–v0.7 的证据仍按原模板、协议和切点解释；v0.8 以 P0–P4 及两个新增代表切点验证持续工作，不扩大旧结论。专业外部合理性需要另行校准，内核测试不替代它。


## v0.9：模板推动的共同审阅关系

[新审计](docs/reference/template-expansion-audit.md)要求在不同工作结构中验证内核。财务集合核对及有限报告正文评价属于领域；公开匹配、撰写、审阅和修复属于策略。它们使用同一JSON对象、版本、项目、采用、发布与运行器，不增加模板名驱动的核心分支。

`issues`、`issue_responses`、`issue_decisions`是三个追加的机构事实登记。问题固定项目、工作/需求、提交、对象版本、位置、提出者及证据；回应关联该问题和同一工作的一次真实提交；有权人的处理决定接受修复、接受反驳或保留问题。决定记录其确切回应与提交，不宣称自由文本意见或机构接受等于领域真值。

创建时指向当前待审提交的问题可以约束该工作版本的批准；已存在问题不因文件编辑或重新提交消失。新意见若到达时原提交已过期，仅保留历史，不阻塞新提交或转移到替代工作。批准条件与资料获取条件分别投影：未处理问题不禁止本人withdraw/edit/resubmit。每次决定只处理一个问题，不能用别的问题回应或过期提交回应关闭它。已接受的处理事实不被后续评价篡改，也不能当作对后来所有内容的质量背书。

`inspect_submission`公开固定提交范围；实际定位须读取确切版本。JSON位置可为键及数组下标，它依赖精确版本而非可变当前位置。文件适配层校验位置，领域判断内容，核心记录关系与作用域。原始问题/回应/决定身份及负载保持；同key同负载不重复，改变负载拒绝。

工具拒绝由知道被违反合同的边界标注类别和原因码；保留type/message及actor/action等实际上下文。参数、能力、业务约束和实现异常分开；尚未赋予结构化语义的错误保留unknown。策略不能仅按`ok=false`推断环境故障。旧v0.8暂停原始事实保持，新版程序策略明确将其归为业务约束。
